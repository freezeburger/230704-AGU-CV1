import { UiZoomableDirective } from './ui-zoomable.directive';

describe('UiZoomableDirective', () => {
  it('should create an instance', () => {
    const directive = new UiZoomableDirective();
    expect(directive).toBeTruthy();
  });
});
